import 'package:flutter_test/flutter_test.dart';

void main() {
  test('library can be imported', () {
    // Public API surface is verified in the per-widget test files.
    expect(true, isTrue);
  });
}
